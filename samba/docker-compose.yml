x-arcane:
  icon: https://www.oschina.net/uploads/img/200901/22150830_bQkl.png
  urls:
    - https://www.samba.org
services:
  samba:
    image: dockurr/samba
    container_name: samba
    environment:
      NAME: ${SMBNAME}
      USER: ${SMBUSER}
      PASS: ${SMBPASS}
    # Optional, default true
      RW: true
    # Optional, default 1000
      UID: ${UID}
    # Optional, default 1000
      GID: ${GID}
    ports:
      - 445:445
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - samba:/storage
    restart: always
    restart: unless-stopped
    labels:
      com.getarcaneapp.arcane.icon: https://www.oschina.net/uploads/img/200901/22150830_bQkl.png
volumes:
  samba:
    name: smb-volume
